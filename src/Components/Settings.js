import React from 'react'

class Settings extends React.Component {
    state = {  } 
    render() { 
        return (
            <>
                Settings is showing using React Routing
            </>
        );
    }
}
 
export default Settings;